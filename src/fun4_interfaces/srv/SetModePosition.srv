# Request
int32 mode                    # Mode integer
geometry_msgs/Point position   # Position with x, y, z coordinates

---
# Response
bool success                   # Indicates if the service was successfully processed
string message                 # Additional message or feedback
geometry_msgs/Point position   # Position with x, y, z coordinates
